Terminator   = "";
LockTimeout  = 500;
ReplyTimeout = 100;
ReadTimeout  = 100;
WriteTimeout = 100;
MaxInput     = 3;
ExtraInput   = Error;

wCtrl {
  out ${1} 2 "%02r"; 
}

rCtrl {
  out ${1} 2; 
  in  "%*01r%02r";
}

rConv {
  out ${1} 1;
  in  "%*01r%02r";
}

